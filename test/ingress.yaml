apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: basic-ingress
  annotations:
    kubernetes.io/ingress.global-static-ip-name: "ingress-example1"
spec:
  defaultBackend:
    service:
      name: nginx-ingress
      port:
        number: 80
---